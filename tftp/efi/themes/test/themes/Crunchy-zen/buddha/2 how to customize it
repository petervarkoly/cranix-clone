### BACKGROUND IMAGE ###
if you use directly an image, it could corupt your grub menu (it'll reboot instead of showing your menu).

DO USE THE CONVERT FUNCTION TO BE SURE THAT YOUR GRUB WILL STILL BOOT

source of the image: http://www.desktopwallpapers4.me/photography/zen-18445/

for example:
sudo convert ~/Downloads/"18445-zen-1920x1080-photography-wallpaper.jpg" -resize 1920x1080! -sharpen 5.0x1.3 -quality 80 /boot/grub/themes/Crunchy-zen/buddha/zen-buddha.jpg

The sudo is only to have the right to write a file in the /boot folder.

### FONTS ###
the font, in grub, can be loaded only in one size per file. You can make the pf2 file with that command:

sudo grub-mkfont -s 19 -o /boot/grub/themes/Crunchy-zen/PoiretOne_normal.pf2 /usr/share/fonts/truetype/Poiret_One/PoiretOne-Regular.ttf -v
or
sudo grub-mkfont -b -s 32 -o /boot/grub/themes/Crunchy-zen/PoiretOne_title.pf2 /usr/share/fonts/truetype/Poiret_One/PoiretOne-Regular.ttf -v

-b: bold
-s: size
-o: output
-v: verbose (necessary to know how to call the font in theme.txt)

The font can be found here: https://fonts.google.com/specimen/Poiret+One

Source for more than 800 fonts : https://fonts.google.com/
